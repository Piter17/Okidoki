name: Deploy Flutter Web to Cloudflare Pages

on:
  push:
    branches:
      - main
      - stage

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    defaults:
      run:
        working-directory: app

    env:
      API_URL: ${{ secrets.API_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build runner
        run: dart run build_runner build --force-jit

      - name: Select env
        id: project
        run: |
          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "API_URL=https://api.okidoki.fun" >> $GITHUB_ENV
            echo "name=okidoki" >> $GITHUB_OUTPUT
          else
            echo "API_URL=https://api-dev.okidoki.fun" >> $GITHUB_ENV
            echo "name=okidoki-test" >> $GITHUB_OUTPUT
          fi

      - name: Build web
        run: flutter build web --release --wasm --dart-define=API_URL=$API_URL

      - name: Deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./app/build/web --project-name=${{ steps.project.outputs.name }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}